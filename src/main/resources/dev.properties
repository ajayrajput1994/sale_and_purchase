########################################
# 🌐 Server Configuration
########################################
server.servlet.context-path=
server.error.whitelabel.enabled=false

# Session settings
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.servlet.session.tracking-modes=cookie
server.servlet.session.persistent=true
server.servlet.session.store-dir=${java.io.tmpdir}

########################################
# 🗄️ Datasource Configuration
########################################
spring.datasource.name=olx
spring.datasource.url=jdbc:mysql://localhost:3306/olx?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

########################################
# 🧠 JPA & Hibernate Configuration
########################################
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Debug SQL output (commented)
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

########################################
# 🔐 Spring Security Configuration
########################################
spring.main.allow-bean-definition-overriding=true

# Debug security logs (commented)
# logging.level.org.springframework.security=DEBUG  
# logging.level.org.springframework.security.web.authentication=DEBUG

########################################
# 🧵 Session Management
########################################
# JDBC session store
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.jdbc.schema=classpath:org/springframework/session/jdbc/schema-@@platform@@.sql
spring.session.jdbc.table-name=SPRING_SESSION

# Redis session store (optional)
# spring.session.store-type=redis

########################################
# 🐞 Debug Logging (commented)
########################################
# logging.level.org.springframework.jdbc.core=DEBUG
# logging.level.org.springframework.jdbc.datasource=DEBUG
# logging.level.org.springframework.transaction=DEBUG
# logging.level.com.olxseller.olx.config=DEBUG

#enabling multipart uploads
spring.servlet.multipart.enabled=true

#max file size limit 
spring.servlet.multipart.max-file-size=30MB
spring.servlet.multipart.max-request-size=50MB

#threshold after which file return to disk
spring.servlet.multipart.file-size-threshold=1KB

# imageLocation=D://onetoz-image//
imageLocation=src/main/resources/static/image

razorpay.keyID=rzp_test_AxAZNCXYzwYinS
razorpay.secreteKey=QaAiRHOX2WPFrUp9IPN6IpCM